#!/usr/bin/env node
/**
 * CLI entry point for local execution via npx
 * This allows the MCP to run on the user's machine even when installed from Smithery
 */

// Simply import and run the main index file
import './index.js';
